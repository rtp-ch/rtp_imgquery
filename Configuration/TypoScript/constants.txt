
// The layout of the HTML/JavaScript code which is inserted for responsive image
styles.content.breakpoints.layout = EXT:rtp_imgquery/Resources/Private/rtp_imgquery.min.html

// The default style of responsive images (image size is fluid)
styles.content.breakpoints.style = width: 100%; height: auto;

// The default breakpoint (screen width), i.e. the breakpoint which corresponds to
// the default image width.
styles.content.breakpoints.default =

// Default breakpoints for images in "Text with images" and "Images only content elements. Formatted
// as breakpoint:width, breakpoint:width
styles.content.breakpoints =
