# *****************
# CType: image
# *****************
# (also used for rendering 'textpic' type):

tt_content.image.20.1 {
    breakpoint = TEXT
    breakpoint {
        value = {$styles.content.breakpoints.default}
        override.cObject = TEXT
        override.cObject.field = tx_rtpresponsive_breakpoint
    }

    breakpoints = TEXT
    breakpoints {
        value = {$styles.content.breakpoints}
        override.cObject = TEXT
        override.cObject.field = tx_rtpresponsive_breakpoints
    }

    breakpoints.layout = {$styles.content.breakpoints.layout}
    breakpoints.debug = {$styles.content.breakpoints.debug}
    breakpoints.style = {$styles.content.breakpoints.style}
}